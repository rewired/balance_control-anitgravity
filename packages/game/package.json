{
  "name": "@balance-control/game",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "test": "vitest"
  },
  "dependencies": {
    "@balance-control/rules": "workspace:*",
    "@balance-control/shared": "workspace:*",
    "boardgame.io": "^0.50.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
     "typescript": "^5.0.0",
     "vitest": "^0.30.0"
  }
}
