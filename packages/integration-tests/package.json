{
  "name": "@balance-control/integration-tests",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "test": "vitest run --no-threads --sequence.concurrent false",
    "golden:update": "node scripts/update-golden.mjs --write",
    "golden:check": "node scripts/update-golden.mjs --check",
    "test:matrix": "vitest run --no-threads --sequence.concurrent false test/cross-expansion-matrix.test.ts"
  },
  "dependencies": {
    "@balance-control/game": "workspace:*",
    "@balance-control/packs": "workspace:*",
    "@balance-control/expansion-01": "workspace:*",
    "@balance-control/expansion-02": "workspace:*",
    "@balance-control/expansion-03": "workspace:*",
    "@balance-control/rules": "workspace:*",
    "boardgame.io": "^0.50.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^0.34.6"
  }
}
