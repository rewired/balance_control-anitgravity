{
  "name": "balance-control-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:report": "playwright show-report",
    "e2e:serve:dev": "concurrently -k -s first -n server,client \"pnpm -C packages/server run dev\" \"pnpm -C packages/client-web run dev --port 4173 --strictPort\"",
    "e2e:serve:ci": "concurrently -k -s first -n server,client \"pnpm -C packages/server run start\" \"pnpm -C packages/client-web run preview --port 4173 --strictPort\"",
    "test:ui:unit": "pnpm -C packages/client-web test",
    "test:ui:coverage": "pnpm -C packages/client-web test:coverage",
    "test:ui:e2e": "pnpm -w e2e",
    "test:ui:all": "pnpm run test:ui:coverage && pnpm run test:ui:e2e",
    "test": "pnpm run check:spec-anchors && pnpm run verify:ui-interaction && pnpm -r --if-present test",
    "lint": "eslint \"packages/**/*.{ts,tsx,js,cjs,mjs}\" \"scripts/**/*.{js,cjs,mjs}\" \"*.{js,cjs,mjs}\"",
    "check:encoding": "node scripts/check-encoding.mjs",
    "gen:spec-anchors": "node scripts/gen-spec-anchors.mjs",
    "audit:spec": "pnpm run gen:spec-anchors && pnpm run check:spec-anchors && pnpm run verify:packs && pnpm run audit:core-obligations && pnpm -C packages/game test -- spec-anchor-tripwire.test.ts && pnpm -C packages/game test -- computeMajorirty.test.ts moves.test.ts hotspot.test.ts && pnpm -C packages/integration-tests test -- golden-replay.test.ts",
    "audit:core-obligations": "node scripts/audit-core-obligations.mjs",
    "dev": "pnpm -r --parallel dev",
    "verify:packs": "node scripts/verify-packs.mjs",
    "check:spec-anchors": "node scripts/check-spec-anchors.mjs",
    "verify:docs": "node scripts/verify-docs.mjs",
    "verify:task": "node scripts/verify-task.mjs",
    "verify:ui-interaction": "node scripts/verify-ui-interaction.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@balance-control/game": "workspace:*",
    "@balance-control/packs": "workspace:*",
    "@types/node": "^20.0.0",
    "@typescript-eslint/parser": "^7.18.0",
    "concurrently": "^9.2.1",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "vitest": "^0.30.0"
  },
  "engines": {
    "node": ">=18"
  }
}
