# Hex Tile Simulator (static)

Open `hex_tile_simulator.html` in a browser.

## Notes

- The overlay is applied with `mix-blend-mode: luminosity` at opacity 0.8.
- The simulator expects assets in `./assets/`.

